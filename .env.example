# Copy to `.env` and set values before build/deploy (DO NOT commit real secrets)
# Use placeholders here. For production, set actual secrets in Netlify site settings (Site → Settings → Build & deploy → Environment).
# Client-side flags (VITE_) are injected at build time. Avoid placing secret values in VITE_ variables.

# Example local development (only for local testing; don't commit):
# VITE_GEMINI_API_KEY=your_client_gemini_key_here   
# avoid using this in production builds
VITE_USE_GEMINI_PROXY=true

# Optional: custom Gemini endpoint (if you need to override)
# VITE_GEMINI_ENDPOINT="Put Your Custom Gemini Endpoint Here"

# If you want the client to post directly to an Apps Script (NOT recommended - CORS & exposes URL):
# VITE_SHEETS_ENDPOINT="Put Your Sheets Web App URL Here"

VITE_USE_SHEETS_PROXY=true
